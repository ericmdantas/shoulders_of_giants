language: node_js

node_js: "0.11"

before_script:
  - npm install -g bower gulp-cli mocha
  - npm run-script test-on-travis
  - bower install
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script:
  - gulp test
  - npm run-script test-on-travis

services: mongodb